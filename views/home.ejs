<%- include("partials/header") %>

<h1>Home</h1>
<p> <%= homeStartingContent %> </p>

<% contentArray.forEach(element => { %>
    <h1> <%= element.title %> </h1>
    <p> <%= element.paragraph.substring(0, 100) + "..." %> <a href="/posts/<%= element.title %>">Read More</a></p>
<% }); %>


<%- include("partials/footer") %>